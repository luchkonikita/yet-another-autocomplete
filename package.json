{
  "name": "yet-another-autocomplete",
  "version": "0.0.9",
  "description": "Yet another autocomplete for your browser JS",
  "main": "./dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/luchkonikita/yet-another-autocomplete"
  },
  "author": "luchkonikita@gmail.com",
  "license": "MIT",
  "exclude": [
    "node_modules"
  ],
  "scripts": {
    "dev": "tsc -p ./tsconfig.json -w",
    "build": "tsc -p ./tsconfig.json",
    "test": "jest --config .jest.json",
    "test-update": "jest --config .jest.json -u",
    "lint": "tslint --project ./"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^22.1.3",
    "jest": "^22.4.2",
    "ts-jest": "^22.0.4",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2"
  }
}